apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: airflow-dev
  namespace: kommander-flux

spec:
  interval: 10m
  prune: true

  sourceRef:
    kind: GitRepository
    name: apache-airflow-nkp

  path: ./clusters/dev